export PATH="$PATH:$HOME/bin"

alias dfs='dotfiles'

alias dfsu='dfs pull && ansible-playbook $HOME/dotfiles/setup-environment.yml --ask-become-pass'

# Directories move 
alias ydir='pwd | xclip'
alias pdir='xclip -o'
alias odir='cd `pdir`'
